# __SLUG__

Generated from python-batch 1.0.0. Synthetic inputs only.

From this directory, with Python 3.11+ and uv 0.12.11 available:

```
uv sync --locked --offline
uv run --locked --offline python -m unittest discover -s tests -v
```

The project has no third-party dependencies. Its lock is bundled with the template.
The job prints a deterministic JSON summary of __ROWS__ synthetic rows.
Offline validation checks archive integrity, approved content and static syntax.
It does not run this code, contact a workspace, select compute, or establish
workspace validity. Compute, workspace identity, CI publication and deployment
workflows must be reviewed in later integration tasks. No schedules are generated.
